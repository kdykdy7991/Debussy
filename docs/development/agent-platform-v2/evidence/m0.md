# M0 验收记录

## 候选 1：第一轮联合审查

里程碑：M0 契约与版本一致性

候选提交：

- 后端：`93b2b57`、`0408b88`
- 前端：`656a5d8`、`e4aea33`
- 验收合并：`32072f1`、`b43dfbe`

日期：2026-08-24

负责人：总架构师

环境与特性开关：验收 worktree；候选仅涉及契约、文档与 SDK 原型测试，`PI_AGENT_V2_METRICS` 尚未实现或启用。

数据库迁移版本：无。候选建议扩展现有 `conversation_events.payload` JSONB，待 M1 接线前批准最终 payload 结构。

### 变更范围

- Metrics/Context DTO、推导函数和错误码候选。
- 指标与上下文 API 示例及五组 fixture。
- M0 ADR D1～D5 草案。
- 前端代码现状、候选方案和 20 条契约问题。
- Embed SDK 行为原型测试及缺口报告。

### 自动化验证

| 验证 | 结果 | 证据 |
| --- | --- | --- |
| Protocol build 类型检查 | 通过 | `tsgo --noEmit -p packages/protocol/tsconfig.build.json` |
| Protocol test 类型检查 | 通过 | `tsgo --noEmit -p packages/protocol/tsconfig.test.json` |
| Protocol 测试 | 通过 | 10 files、330 tests passed |
| Server reasoning 回归 | 通过 | `model-parameters.test.ts`，9 tests passed |
| 既有 SDK + M0 原型 | 不通过 | 31 tests 中 1 failed：`height == 0` 被透传 |
| 后端文档所列 typecheck 命令 | 不可执行 | protocol package 没有 `typecheck` script，需改为两个 tsconfig 的 `tsgo --noEmit` |
| Biome 候选文件检查 | 不通过 | 后端两个 TS 文件通过；前端 `sdk-m0-prototype.test.ts` 缺少文件末尾换行 |

安装依赖时存在既有警告：Node `22.23.2`，间接依赖 `@earendil-works/gondolin@0.12.0` 声明要求 Node `>=23.6.0`；npm audit 报告 4 moderate、3 high。本轮未新增依赖，警告不归因于候选，但在进入内部可用前仍需单独处理。

## 已批准的架构决定

以下决定可以立即作为修订依据，不需要再次讨论：

### A1：M0 不修改 Runtime

批准 ADR D1。M0 只允许契约、fixture、原型和测试；`pi-runtime-adapter.ts`、`turn-executor.ts` 与正式采集留到 M1，且必须在修订后的 M0 通过后开始。

### A2：事件日志继续作为唯一事实源

有条件批准 ADR D2。Metrics 放入现有终止事件 payload，不新增第二事实表；必须保持现有 `{ ok, usage }` 兼容结构，建议新增嵌套字段 `metrics`，不得用 `TurnMetrics` 覆盖整个 payload。M1 查询性能不足时可增加非关键索引，但须先提交 explain 证据。

### A3：事件采用斜杠式新写入，legacy 只读

有条件批准 ADR D3。所有新事件使用 `SESSION_EVENT_TYPES` 的斜杠式名称；legacy 点式事件仅用于读取存量。后端必须先补齐成功、失败、取消的权威终止事件映射和兼容期限，再视为冻结。

### A4：Metrics 开关默认关闭

批准 `PI_AGENT_V2_METRICS=false`。不批准提前冻结未使用的 `PI_AGENT_V2_REASONING`：reasoning 已是现有能力，除非 M1 提交明确灰度需求、控制边界和测试，否则不得增加无实现的环境变量。

### A5：迁移只向前

批准 ADR D5。回退采用关闭开关和保持向后兼容的数据结构，不写破坏性 down migration。

### A6：Embed resize 的有效范围

`height` 必须是 `1..POST_MESSAGE_RESIZE_MAX_HEIGHT` 的整数；`0` 不代表折叠，必须在 protocol decode 边界拒绝。高度上限以 protocol 常量为唯一来源，SDK 不复制第二个 magic number。

### A7：Launch Token 的内存边界

销毁或首次发送后断开字符串引用即满足当前要求；不采用覆写为 `"\0"` 的伪清零方案。硬性要求仍是 Token 不进入 URL、DOM、持久化存储、日志或错误上报。

### A8：Embed SDK 包与公开 API

M1 正式 package 使用 `@earendil-works/pi-embed-sdk`，公开命名导出 `createEmbed` 和 `EmbedInstance`，不使用 default export。postMessage 首期保持 v1 兼容扩展；没有明确破坏性需求不得升 v2。正式 package 创建属于 M1，不在 M0 原型中实施。

### A9：Fixture 适配边界

fixture 只存在于测试/开发入口，通过单一 typed adapter 暴露与真实 API 相同的读模型；不得进入生产 bundle，不在组件内散落 mock 分支，不使用占位业务数字。前端可在第一批 DTO 冻结后建立适配层。

### A10：内部网络范围

发布对话页只服务团队或企业内部成员，不规划公网。移动端验证仍覆盖电脑 Chrome、Android Chrome 和 iOS Safari，但不引入公网部署、外部 SLA 或外部客户容量要求。

## 阻断项

### B1：轮次结果和聚合样本语义不完整

负责人：后端

`ConversationTurnMetric` 缺少成功/失败/取消结果，`TurnMetrics.totalLatencyMs` 又恒为 number；当前 `sampleCount` 的过滤条件永远为真，无法实现“只统计有值成功轮次”。修订要求：

- 增加明确的 `outcome`，或提供等价且可验证的终止状态；
- 明确失败、取消、Tool-only 的显示和聚合规则；
- 所有聚合字段只纳入契约允许的样本；
- 增加失败、取消、无文本和混合样本测试。

### B2：单调耗时与墙上时间混用

负责人：后端

当前 `deriveTurnMetrics` 用 epoch ms 同时生成 ISO 时间并计算耗时，不满足“单调时钟计算耗时”。修订要求：展示/审计时间使用 wall clock，TTFT/generation/total latency 使用 monotonic duration；定义二者采集点和持久字段，增加墙上时间回拨不影响耗时的测试。

### B3：终止事件 payload 兼容性未冻结

负责人：后端

现有 `turn/end` payload 包含 `ok` 和 `usage`。修订契约必须明确 `metrics` 的嵌套位置，证明 Usage 聚合仍读取原路径，并定义失败/取消终止事件是否携带 metrics。

### B4：空状态与错误码冲突

负责人：后端；总架构师复核

API 示例使用 `200 + available=false` 表达旧会话无数据，错误码表又定义 404 `METRICS_UNAVAILABLE`/`CONTEXT_SNAPSHOT_UNAVAILABLE`。冻结语义为：

- 已存在会话但无历史数据：HTTP 200，`available=false`；
- 会话不存在或跨租户：统一 HTTP 404，不泄露归属；
- 服务故障：HTTP 503；
- 非法过滤：HTTP 422。

后端应删除未使用的 unavailable 错误码，或提出不冲突的新用途并重新评审。

### B5：逐轮列表缺少分页

负责人：后端

`items[]` 不允许无界返回。补齐 cursor/sequence、排序方向、默认/最大 page size、下一页标志和非法 cursor 语义；fixture 与测试必须覆盖至少两页。

### B6：事件失败/取消命名与 legacy 边界未完成

负责人：后端

现有代码仍读取/统计 `turn.failed` 等点式事件，而权威枚举包含 `turn/end`、`turn/interrupted`。必须形成成功、失败、取消映射表，明确新写入名称、旧读取名称、审计/统计口径和兼容期限。

### B7：M0 契约状态标记过早

负责人：后端

总架构师尚未批准时，`m0-contract` 不得写“状态：可开发”“冻结 DTO”“M0 已批准”。改为“候选/待评审”，ADR 的“后端自检”须有实际勾选和提交证据；本轮通过后再统一改为冻结。

### B8：M0 其余契约尚未提交

负责人：后端提出；总架构师批准；前端复核可消费性

当前只覆盖 Metrics/Context。仍需处理前端问题清单中的 reasoning 会话持久化、Skill DTO、MCP DTO、Secret 替换、Tool allowlist、统一错误信封和 API 权限。可以分为 M0-A/M0-B 候选审查，但进入对应 M1/M2/M3 实现前必须先冻结该能力契约。

### F1：SDK 多实例测试未真实共享 window

负责人：前端

当前 `envA = makeEnv()`、`envB = makeEnv()` 生成两个独立 FakeWindow，不能证明同一宿主页上的两个实例互不串扰。修订为两个 iframe/实例共享同一个 FakeWindow 和 handler 数组，并验证 A.destroy 不移除 B 的监听器。

### F2：原型提交使测试集合保持红灯

负责人：前端

报告声称用 `it.fails()` 标注缺口，实际测试使用普通 `it()`，导致指定测试失败。架构决定 A6 已明确拒绝高度 0；前端应先在 protocol 增加边界测试，由后端/共享契约负责人修复 decoder，再让 SDK 原型测试转绿。任何合入共同基线的测试必须为绿，不能把预期缺口作为普通失败测试提交。

### F3：原型报告链接与范围描述需修订

负责人：前端

修复报告中错误的相对链接；将“SDK 正式 package 属于 M1/M2 边界外”改为架构决定 A8 的 M1；删除覆写 Launch Token 字符串的建议；按内部网络范围修改示例 origin。

### F4：前端候选格式检查未通过

负责人：前端

`sdk-m0-prototype.test.ts` 缺少文件末尾换行，联合候选 Biome 检查失败。修订提交必须通过该具体测试文件的 Biome 检查，并同步修复两个新增 Markdown 文件的 EOF 换行。

## 对前端契约问题的阶段性答复

- Q1：Metrics/Context 是 Control Plane 管理接口，使用管理员鉴权；不是 Conversation-scoped token。
- Q2：服务端返回聚合和分页逐轮明细；前端不自行聚合。仅纳入成功且字段有值的样本。
- Q3：旧会话由服务端返回 `available=false`；前端不根据 `createdAt` 推断。
- Q4：不可归类内容必须新增明确字段并升级契约，不放入 `conversationMessages`。
- Q5：首版模型目录保持现有 reasoning capability；没有实际参数时不增加通用步长/互斥字段。
- Q6：会话 reasoning 覆盖必须服务端持久化；具体存储/API 由后端第二批候选提出。
- Q7：Admin 与 Embed 可以使用不同路由和权限，但必须共享同一服务语义、校验器和错误码。
- Q8～Q15：等待后端 Skill/MCP 第二批契约候选，不允许前端先定义。
- Q16：按 A8 冻结为 `@earendil-works/pi-embed-sdk`、`createEmbed`、`EmbedInstance`。
- Q17：控制台示例与 SDK 测试/文档使用同一源码片段或生成来源，避免双份手写漂移；默认展示 mount、ready、error、destroy。
- Q18：Admin 控制接口统一沿用 `{ error: { code, message, requestId, retryable } }`；Embed 保持其受限错误协议，不强行共用 Admin 信封。
- Q19：DTO 变化先运行 protocol build/test 两套 tsconfig 类型检查和指定 protocol 测试；不存在的 `npm run typecheck --workspace=@earendil-works/pi-protocol` 不得写入文档。
- Q20：第一批 Metrics/Context 修订候选应在本轮整改后立即提交；Skill/MCP 可以分批冻结，但不得越过门禁实现。

## 安全与隔离检查

- 本轮未发现 Token 持久化；原型验证 localStorage、sessionStorage、cookie 均未写入。
- 跨租户继续采用统一 404，不暴露资源归属，方向批准。
- MCP/Skill/Secret 尚无候选，不能视为通过。

## 性能与回退

- Metrics 默认关闭和 JSONB 向后兼容方向批准。
- 尚无运行时采集、查询 explain、容量或回退实测；属于 M1，不在本轮声称通过。

## 结论

结论：**不通过，退回修订**。

理由：B1～B8、F1～F4 中存在契约正确性、测试红灯、格式门禁和范围未完成问题。当前候选不得标记“冻结”，后端不得进入 M1 Runtime 接线，前端不得进入依赖正式 DTO 的页面/fixture 实现。

允许继续的工作：

- 后端修订第一批 Metrics/Context 候选，并准备后续 Skill/MCP 契约；
- 前端修订 SDK 原型测试和报告，但不创建正式 package、不实现依赖 DTO 的页面；
- 总架构师对修订候选进行第二轮审查。

产品负责人决定：退回修复。

## 候选 2：第二轮联合审查

候选提交：后端 `f64b773`；前端 `3fc3f74`；验收合并 `91fc49e`、`84d1015`。

日期：2026-08-24

### 自动化验证

| 验证 | 结果 | 证据 |
| --- | --- | --- |
| Protocol build/test 类型检查 | 通过 | 两套 `tsgo --noEmit` 均退出 0 |
| Metrics 契约 + reasoning 回归 | 通过 | 2 files、29 tests passed |
| 既有 SDK + M0 原型 | 有缺口 | 31 passed、1 skipped；跳过项正是已冻结的 `height == 0` 拒绝边界 |
| Biome 候选 TS 文件 | 通过 | 3 files checked，无修复 |
| Patch/EOF 检查 | 通过 | `git diff --check` 无输出；修订报告含末尾换行 |

### 已关闭项

- B1：新增 `outcome`，失败/取消不进入成功样本聚合，并补混合样本测试。
- B2：墙上时间与单调耗时已拆分，派生公式不再使用 ISO 时间戳减法。
- B3：`turn/end` 明确保留 `ok`/`usage`，顶层可选追加 `metrics`。
- B4：空态 200、子系统不可用 503、非法过滤 422 已分开。
- F1：多实例测试已使用同一个宿主 window 和不同 iframe。
- F3：SDK 包名、公开 API、v1 兼容、Launch Token 与内部网络范围已按裁决修订。
- F4：候选 TS 格式和新增 Markdown EOF 已通过检查。

### 第二轮阻断项

#### R1：分页契约仍不完整（B5 未关闭）

负责人：后端

当前只在响应增加 `nextAfterSequence`，仍缺请求 DTO/查询参数、默认及最大 `pageSize`、
非法/越界 cursor 语义和至少两页 fixture/测试。更严重的是
`computeConversationMetricsStats(items)` 直接对当前页计算 `available`、均值与分位：后续空页会把
实际有历史指标的会话误报为 `available=false`，不同分页也会得到不同“会话统计”。本产品口径
冻结为：**`stats` 是全会话聚合，分页只作用于 `items`**。

#### R2：失败事件不在权威枚举中（B6 未关闭）

负责人：后端

运行时代码新写 `turn/failed`，映射函数也把它归为 failed，但 `SESSION_EVENT_TYPES` 不包含
`turn/failed`。文档同时写“失败当前以 `turn/interrupted` 表达”，与实现矛盾。冻结要求：新写入
失败使用 `turn/failed`，取消使用 `turn/interrupted`，成功使用 `turn/end`；把失败事件纳入权威
枚举及验证，点式名称只读兼容，并同步修正文档。

#### R3：契约状态和已否决开关仍未修正（B7 未关闭）

负责人：后端

候选仍标记“状态：可开发”“冻结 DTO”“M0 已批准”，ADR 后端自检也未勾选；这些表述在本轮
未通过前不成立。`PI_AGENT_V2_REASONING` 已在 A4 明确不批准，候选仍将其写为预留并声称命名
冻结，必须删除。ADR D3 的“括号式”是笔误，应为“斜杠式”。

#### R4：类型与数值校验边界不合格

负责人：后端

`ConversationTurnMetric.sessionEffort` 从既有 `ReasoningEffort | null` 弱化为 `string | null`，
`TurnEndPayload.usage` 又复制成 `Record<string, unknown>`。应复用既有强类型，或给出等价严格
schema。还需校验单调延迟的非负及顺序关系，防止 `firstOutputDelayMs > totalElapsedMs` 产生负
`generationMs`。

#### R5：其余 M0 契约仍未交付（B8 未关闭）

负责人：后端提出；总架构师批准；前端复核

ADR 已明确 Skill/MCP/reasoning 持久化“尚未提交”。因此 Metrics/Context 第一批候选即使修完，
也只能作为 M0-A 冻结，不能宣告完整 M0 通过，更不能越过对应门禁进入 Skill/MCP 实现。

#### R6：已冻结的 resize 边界被跳过（F2 未关闭）

负责人：共享 protocol 由后端修复；前端启用回归用例

A6 已决定 `height` 有效范围为 `1..POST_MESSAGE_RESIZE_MAX_HEIGHT`，且必须在 protocol decode
边界拒绝 0。把用例改为 `it.skip` 只是让测试退出 0，并未完成修复。应修改 decoder、增加
protocol 边界测试、移除 SDK 原型的 `skip`；上限继续只引用 protocol 常量。

### 第二轮结论

结论：**不通过，退回第二次修订**。

本轮关闭了 7 项首轮问题，自动化也没有普通失败测试；但 R1～R6 仍包含分页统计错误、事件
权威枚举冲突、已否决开关、共享类型弱化、完整 M0 范围缺失，以及通过 `skip` 掩盖冻结边界。
当前不得把 DTO 标记为冻结，前后端不得从该候选创建 M1 正式实现分支。

下一候选最小范围：

1. 后端修 R1～R4，并提交 Skill/MCP/reasoning 持久化的 M0-B 契约，或正式把里程碑拆为
   M0-A/M0-B 并更新总计划依赖；
2. 后端修 protocol 的 resize 0 边界，前端移除 `skip` 后复跑 SDK 测试；
3. 前端可继续不依赖 DTO 的页面骨架与状态壳，但不得建立声称基于“冻结 DTO”的正式适配层；
4. 总架构师只审查新的修订提交，不以修改旧候选的方式代替负责人交付。

## 候选 3：前端专项第三轮审查

候选提交：前端 `f92f0c3`；验收合并后基线包含该提交。

日期：2026-08-24

### 自动化验证

| 验证 | 结果 | 证据 |
| --- | --- | --- |
| Protocol build/test 类型检查 | 通过 | 两套 `tsgo --noEmit` 均退出 0 |
| Protocol 全套测试 | 通过 | 10 files、341 tests passed |
| 既有 SDK + M0 原型 | 通过 | 2 files、35 tests passed，无 skip |
| Biome / patch 检查 | 通过 | 3 files checked；`git diff --check` 无输出 |

### 审查结论

结论：**前端专项不通过；R6 部分关闭，R1～R5 仍由后端候选负责。**

确认有效的改动：protocol decoder 已把 resize 合法范围收紧为
`1..POST_MESSAGE_RESIZE_MAX_HEIGHT`，SDK 原型不再 skip，`height == 0` 的端到端行为测试已转绿。

仍需前端修订：

1. SDK 又用 `decoded.message.height <= 0 || ... > EMBED_HEIGHT_MAX` 做第二套范围判断，仍保留
   `EMBED_HEIGHT_MAX` magic number，违反 A6“protocol decode 边界和常量为唯一来源”。SDK 的 resize
   分支应信任成功 decode 的 typed message，只负责分发与同步高度。
2. Protocol 边界改变没有在 `packages/protocol/test/embed/post-message.test.ts` 增加 `height=0`
   的直接单元测试。Web 集成测试不能替代共享 decoder 的契约测试。
3. 新增的“单调事件顺序”测试只验证同步 `dispatch` 的调用顺序，与后端 monotonic clock、墙钟
   回拨和指标推导无关；不得作为 R4 单调延迟校验已经处理的证据。

### 文档契约越界与错误

`contract-questions.md` 可以记录前端消费诉求，但本提交自行提出的 DTO 与已批准口径冲突，不能
合入冻结契约：

- 把 ISO 8601 `requestStartedAt`/`capturedAt` 称为“单调时钟”；ISO 时间是 wall clock，不能用于
  monotonic duration，也不能用 ISO 字段相减作为指标公式。
- 用 `status: succeeded|tool_only|failed|cancelled|retried|interrupted` 替换已批准的
  `outcome: success|failed|cancelled`，并让 tool-only 参与均值；这是未经批准的新产品语义。
- 建议新增 `turn/succeeded`、`turn/cancelled`、`turn/tool_only`、`turn/retried`，与已批准映射
  `turn/end` / `turn/failed` / `turn/interrupted` 冲突。
- 把 R1 错写成 `GET /api/control/v1/conversations` 列表分页；实际阻断接口是
  `/conversations/:id/metrics`，且 stats 必须是全会话聚合、分页只作用于 items。
- Skill/MCP 草案存在 TypeScript 语法错误 `readonly readonly`，并在安全与 Transport 评审前自行
  加入 `stdio`、明文 `endpoint`/`streamableHttpUrl` 等形状；这些只能由后端提交完整 schema、
  权限、错误码、Secret 与迁移边界后评审。
- 文档自行宣告 M1 删除既有 `setThinking`，超出 M0 和前端授权范围；未经调用链审计与迁移计划
  不得删除既有兼容通道。

前端下一提交仅需：移除 SDK 重复范围/magic number；补 protocol 的 0 边界单元测试；删除与
后端时钟无关的“单调事件顺序”认领；把契约文档恢复为消费问题清单，不自行替换已批准 DTO、
事件和后端持久化方案。R1～R5 是否关闭，等待后端新的实现/契约提交，不能由前端文档代办。

## 候选 4：后端专项第三轮审查

候选提交：后端 `cb256ec`；验收合并后基线包含该提交。

日期：2026-08-24

### 自动化验证

| 验证 | 结果 | 证据 |
| --- | --- | --- |
| Protocol build/test 类型检查 | 通过 | 两套 `tsgo --noEmit` 均退出 0 |
| Protocol 全套测试 | 通过 | 11 files、345 tests passed |
| Biome / patch 检查 | 通过 | 8 files checked；`git diff --check` 无输出 |

### 已关闭项

- R2：`turn/failed` 已加入 `SESSION_EVENT_TYPES`，成功/失败/取消的新写入与 legacy 只读映射一致。
- R4 的原始类型问题：`sessionEffort` 恢复为 `ReasoningEffort | null`，`usage` 复用既有
  `Usage`；单调延迟增加有限数、非负和顺序校验，并由测试覆盖乱序拒绝。
- R3 的 reasoning 开关部分：已删除未批准的 `PI_AGENT_V2_REASONING`，只保留
  `PI_AGENT_V2_METRICS=false`。

### 仍阻断冻结

#### H1：分页边界仍未冻结（R1 未关闭）

`ConversationMetricsQuery.limit` 仍写“缺省由实现定义，首版建议 50”，没有确定默认值、最大值、
合法整数范围与超限处理；`afterSequence` 也没有非负整数约束。错误码虽有
`INVALID_METRICS_FILTER`，却没有可执行校验函数/schema 或非法参数测试，也没有首轮要求的两页
fixture/游标测试。冻结建议：默认 50、最大 200，`afterSequence >= 0`、`1 <= limit <= 200`；
非法值统一 422，不做静默钳制。`stats` 全会话、`items` 分页的修订方向正确。

#### H2：Skill 绑定没有固定到 Agent Revision（R5 未关闭）

文档声称“Agent Revision 绑定、发布后不可漂移”，但 `AgentSkillBinding` 只有 `agentId`，没有
不可变 `agentRevisionId`；`SkillBindingRequest` 还允许省略 revision 并取“当前最新”，使并发发布
和重放结果不确定。创建草稿时可以解析 latest，但持久绑定和响应必须返回固定的 Agent Revision
与 Skill Revision 标识。

#### H3：MCP 管理 DTO 无法落实 Secret 和版本边界（R5 未关闭）

`McpServerUpsertRequest.config: Record<string, unknown>` 在类型/解码边界允许任意明文 Secret，
仅靠注释不能满足安全约束；必须改成按获批 transport 区分的严格配置，并只接受 Secret 写入槽位
或一次性替换值，读取 DTO 永不回显。`AgentMcpBinding` 同样只有 `agentId + mcpServerId`，没有
固定 Agent Revision、MCP Revision/schema hash；Tool allowlist 只用不稳定字符串 id，无法证明
发布版本不漂移。

#### H4：未决 transport 被导出为公共候选类型（R5 未关闭）

ADR 说 transport 尚待 BE-3 决定，protocol 却已经导出
`McpTransport = "streamable-http" | "stdio"`，并让所有创建请求依赖它。这实际上提前把 stdio
写进公共契约。M0 只能先冻结与 transport 无关的资源/绑定外壳；具体 config 和 transport union
应等待 BE-3 威胁模型与 ADR，或先批准首期仅 `streamable-http` 后再进入 protocol。

#### H5：reasoning 持久化只有读取状态，没有写入与审计契约（R5 未关闭）

`ConversationReasoningState` 只定义 `{conversationId, effort, updatedAt}`，尚缺更新请求、端点与
权限、稳定错误码、Revision/模型能力校验、审计来源和并发语义。因此可以批准强类型方向，但不能
声称“会话 reasoning 持久化契约已提交完整”；运行时接线属于 M1，不代表 M0 可以省略写契约。

#### H6：候选自检与 ADR 一致性仍未完成（R3 未关闭）

ADR 的“后端自检”仍未勾选；D3 仍写“新事件采用括号式”（应为斜杠式）；D2 仍写“M0 不新增
事件类型”，又在 D7 将 `turn/failed` 加入权威枚举，需改成“不新增第二类指标事件，补登记现有
运行时已写事件”。完成这些文字一致性修订后才可签署 ADR。

### 后端专项结论

结论：**不通过，但属于明显前进，不是重复返工。**

Metrics/Context 的核心结果、时钟、类型、事件和全会话统计方向已基本稳定；下一轮主要补分页
输入边界和 fixture。Skill/MCP/reasoning 已从“完全缺失”推进到管理 DTO 草案，但版本固定、
Secret、transport 与写入审计仍是结构性门禁，不能仅凭接口存在和测试常量数组就冻结。

## 候选 5：前端专项第四轮审查

候选提交：前端 `c3f2700`；验收合并后基线包含该提交。

日期：2026-08-24

### 自动化验证

| 验证 | 结果 | 证据 |
| --- | --- | --- |
| Protocol build/test 类型检查 | 通过 | 两套 `tsgo --noEmit` 均退出 0 |
| 合并基线 Protocol 全套测试 | 通过 | 11 files、352 tests passed |
| 既有 SDK + M0 原型 | 通过 | 2 files、32 tests passed，无 skip |
| Biome / patch 检查 | 通过 | 4 files checked；`git diff --check` 无输出 |

### 审查结论

结论：**前端代码范围通过；前端文档有条件通过；完整 M0 继续等待后端。**

第三轮要求的三个小动作已真实完成：

1. SDK 删除 `EMBED_HEIGHT_MAX` 与第二套范围判断，成功 decode 后只负责 emit/sync；protocol
   decoder 是 resize 合法性的唯一来源。
2. Protocol 新增 decoder 直接边界测试，覆盖 0、负数、上下界、超上界、非整数、非有限数和
   错误类型；Web 集成测试继续覆盖 iframe 不被设置为 0 高度。
3. 与后端 monotonic clock 无关的 SDK“单调事件顺序”测试已删除；前端自拟 DTO、事件、分页、
   Skill/MCP 和删除兼容通道等越界内容已撤回为问题清单。

代码不再退回。进入共同 M0 冻结前只需一次文档同步：

- Q20 和 SDK 报告把已批准包名误写为 `@earendil-works/embed-sdk`，必须恢复为
  `@earendil-works/pi-embed-sdk`；该决定已在 A8 批准，不再作为未决问题。
- §5.7/R2 仍写“`turn/failed` 不在权威枚举”，但后端 `cb256ec` 已完成并经本轮验证；更新为
  已关闭，问题 6 也应直接引用 `turn/end`、`turn/failed`、`turn/interrupted` 的权威映射。
- `m0-survey.md` 中“SDK 端必须保留 `POST_MESSAGE_RESIZE_MAX_HEIGHT`”应改为“protocol 端保留并
  由 SDK decoder 间接消费”，避免被理解为 SDK 再复制常量。

以上都是文档勘误，可与后端最终契约同步提交，不需要新增前端实现或重新设计。前端现在可以
停止 M0 SDK 返工，等待后端契约冻结；仍可继续不依赖 DTO 的页面骨架和状态壳。

## 候选 6：后端专项第四轮审查

候选提交：后端 `251c89d`；验收合并后基线包含该提交。

日期：2026-08-24

### 自动化验证

| 验证 | 结果 | 证据 |
| --- | --- | --- |
| Protocol build/test 类型检查 | 通过 | 两套 `tsgo --noEmit` 均退出 0 |
| 合并基线 Protocol 全套测试 | 通过 | 11 files、359 tests passed |
| Biome / fixture / patch | 通过 | 5 files checked；两份 JSON 可解析；`git diff --check` 无输出 |
| 全仓 `npm run check` | 既有基线失败 | 失败集中在 `packages/agent`、`packages/ai`、`packages/coding-agent` 与 `scripts/smoke-p0.ts` 的 model catalog/模块解析；本候选 protocol 文件未报错 |

`npm run check` 内含 `biome --write`，验收时曾格式化一个非候选 Web 文件；该检查副作用已撤销，
未纳入验收提交。后端工作区报告的 `ai-message-flow.tsx` 未提交改动也未进入候选。

### 本轮批准：Metrics/Context 契约冻结

以下边界已闭环，可作为 **M0-A（Metrics/Context）冻结契约**：

- 全会话 `stats` 与分页 `items` 分离，两页 fixture 的 stats 完全相同；默认 limit 50、最大 200、
  afterSequence/limit 整数边界均有纯函数与测试。
- 接受本候选“limit 超过 200 明确钳制到 200”的公开规则；非正、非整数仍统一返回 422
  `INVALID_METRICS_FILTER`。该规则不同于上轮建议的超限 422，但当前 DTO、文档和测试一致，
  对客户端兼容且无数据正确性风险，架构师批准此调整。
- 成功/失败/取消、slash 新写/legacy 只读、单调耗时、空值、全会话样本、Usage 兼容、空态与
  unavailable、默认关闭及前向回退均已冻结。
- Skill 绑定已固定 Agent Revision；省略 Skill revision 只允许服务端在事务内解析 latest，持久
  绑定和响应必须记录确定的 `skillRevision`。

### 完整 M0 的剩余 3 项结构性阻断

#### J1：reasoning 更新路由与权限平面冲突

同一契约同时写 `PUT /api/control/...` 和“PATCH 语义”，需统一为 PUT。更关键的是 Control Plane
Admin 路由不能同时承载 Embed 会话属主权限。冻结要求：

- Admin 调试：`PUT /api/control/v1/conversations/:id/reasoning`，Admin Token；
- 已发布对话：在 Embed/Data Plane 提供对应 conversation-scoped 更新路由，使用会话 token；
- 两条路由共享同一服务、`ReasoningUpdateRequest`、模型能力校验与错误码；跨租户/跨属主保持
  不泄露归属的 404，只有资源存在但策略禁止调整时才用 403。

#### J2：reasoning 审计动作的事实源不明确

文档说写 `conversation.reasoning-updated`“事件”，但它不在 `SESSION_EVENT_TYPES`，命名也是点式。
必须二选一并写清：若是 conversation event，则加入 slash 权威枚举并定义 payload/schema；若是
独立 control audit log action，则明确不是会话事件、不能用于恢复。会话状态恢复还需明确读取的
持久记录来源，不能只靠一个字符串常量证明可恢复。

#### J3：MCP transport/Secret 边界仍未达到可冻结安全要求

`McpHttpTarget.headers: Record<string,string>` 仍允许调用方放入 `Authorization`、Cookie 等明文
Secret，注释无法执行。首期应删除任意 headers，或定义严格安全 header allowlist；凭据只能走
受租户作用域校验的 Secret ref/slot。与此同时，文档称 transport“未评审”，protocol 却导出
`McpTransportKind = "streamable-http"` 并让创建 DTO 依赖它。BE-3 ADR 必须先完成 URL/SSRF、
DNS 重绑定、私网目标、重定向、超时、响应上限与 Secret 引用授权的威胁模型，再决定是否冻结
首期 streamable-http。此前只能冻结 transport 无关的 MCP 资源和 Agent Revision 绑定外壳。

### 后端第四轮结论

结论：**M0-A Metrics/Context 通过并冻结；完整 M0 仍不通过。**

后端下一轮不再修改 Metrics/Context，除非走契约变更流程。只需提交 J1～J3 的小范围修订和
BE-3 ADR；前端随后做一次 DTO 可消费性复核及三处既有文档勘误。全仓既有类型错误另建基线
治理事项，不归因于本候选，但在最终内部可用门禁前必须恢复全仓 check。
