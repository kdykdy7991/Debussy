# 功能名称

状态：草案

负责人：未指定

创建日期：YYYY-MM-DD

## 1. 背景与目标

说明要解决的问题、目标用户和期望结果。链接对应的官方产品规则，不在这里重新定义产品边界。

## 2. 范围

### 包含

- 明确本次必须完成的能力。

### 不包含

- 明确容易被误认为属于本次工作的内容。

## 3. 当前行为

记录可以从现有代码和运行结果验证的事实。

## 4. 目标行为

按用户操作或系统事件描述完成后的端到端行为。

## 5. 现有方案调研与选型

编码前记录与目标最接近的现有方案，至少检查仓库已有组件/依赖和可用的成熟外部方案。复杂度较高或存在明显第三方方案的能力，不得跳过本节直接自研。

| 候选方案 | 满足程度 | 接入/开发成本 | 维护与活跃度 | 体积/性能 | 安全与许可 | 主要风险 | 结论 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 仓库现有能力 |  |  |  |  |  |  |  |
| 外部方案 |  |  |  |  |  |  |  |
| 自研 |  |  |  |  |  |  |  |

说明检索范围、最小验证结果和最终选择理由。若选择自研，必须明确现有方案不能满足的关键要求；“更灵活”“以后可能需要”不能单独作为自研理由。

止损条件：写明出现哪些效果、工期、维护或兼容信号时暂停当前方案并重新选型。

## 6. 技术方案

说明涉及的 package、模块、协议、数据结构、迁移和关键调用链。优先引用现有代码契约，不复制大段实现。

## 7. 状态与异常

列出加载、空内容、失败、权限、重试、并发、取消和恢复行为。

## 8. 兼容与安全

说明租户隔离、鉴权、敏感数据、兼容性和不可逆变更。

## 9. 实施拆分

按可以独立验证的顺序拆分步骤；多人协作时标明依赖关系，不在此记录每日进度。

## 10. 验收标准

- 使用可观察、可测试的结果描述验收条件。

## 11. 验证方式

- 列出需要运行的类型检查、单元测试、集成测试和人工验证。

## 12. 未决问题

- 只有清空本节、或明确标记为不阻塞后，状态才能改为“可开发”。
